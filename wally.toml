[package]
name = "gigahd/onboot"
description = "A simple Module to load your modules and services"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"

exclude = ["*"]
include = ["src", "src/init.luau", "default.project.json", "LICENSE", "README.md", "wally.toml"]

[dependencies]
